{
  "name": "liteon-demo",
  "version": "0.1.2",
  "private": true,
  "description": "Full SSR demo app — consumes liteon as an installed npm package, exactly like a real project.",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20.6"
  },
  "dependencies": {
    "liteon": "^0.1.2"
  }
}
